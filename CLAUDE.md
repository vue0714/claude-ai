# 프로젝트: 성민의 한글 (Hangul with Seongmin)

영어 사용자를 위한 **무료 한글 학습 사이트**입니다. 무료 학습으로 방문자를 모으고, 유료 자료(전자책, 애니메이션 영상)와 튜터링으로 수익을 내는 구조를 목표로 합니다. 이 문서는 claude.ai 대화에서 만든 첫 버전을 Claude Code로 옮겨 이어서 작업하기 위한 인수인계 문서입니다.

## 운영자 배경
- 블로그, 유튜브, SNS로 한글·한국어 교육과 고대 문화 지식을 전하는 콘텐츠 크리에이터입니다.
- 코딩 경험이 거의 없습니다. 설명은 쉬운 말로 하고, 변경 후에는 무엇이 바뀌었는지 요약해 주세요.
- 대화는 한국어로 진행합니다. 사이트는 영어(기본)와 한국어 두 언어를 지원합니다.

## 사업 방향 (이미 정해진 결정)
1. 사이트의 중심은 **무료 인터랙티브 한글 학습**입니다. 튜터링은 "더 배우고 싶은 사람을 위한 부가 서비스"입니다.
2. 유료 상품은 **전자책과 애니메이션 영상 강의**이며, 아직 준비 중입니다(사이트에는 "준비 중"으로 표시, 가격 없음).
3. 로그인, 진도 저장, 유료 콘텐츠 잠금은 **직접 만들지 않습니다.** 보안과 책임 문제가 커서, 필요해지면 외부 플랫폼(전자책 판매처, 강의 플랫폼)에 맡깁니다.
4. 튜터링 신청은 지금은 이메일로 받습니다. Preply는 보조 채널이며 프로필 주소가 생기면 활성화합니다.
5. 영상 콘텐츠는 유튜브에 무료로 먼저 올려 수요를 확인한 뒤 유료 시리즈로 묶는다는 계획입니다.

## 콘텐츠 원칙
- **정확성이 최우선**입니다. 출처가 불분명하거나 확신이 없는 내용은 확인이 필요하다고 표시하세요.
- 전문 지식이 없는 일반 학습자도 이해하도록 쉽고 흥미롭게 씁니다.
- 한글과 고대 문화 주제는 가능한 깊이 있고 구체적으로 다룹니다.
- `VOWELS`, `CONSONANTS`, `BATCHIM` 데이터의 영어 발음 비교(예: ㅓ = sun의 u)는 대략적인 설명이며, **한국어 발음 전문가의 검수가 필요**합니다.

## 파일 구조
- `index.html` 하나에 HTML, CSS, JavaScript가 모두 들어 있는 정적 사이트입니다(빌드 과정 없음).
- 서버, 데이터베이스, 외부 API가 없습니다. 브라우저에서 파일을 열면 동작합니다.
- 외부 자원은 Google Fonts(Gowun Batang, Noto Sans KR)뿐입니다.
- `assets/` 폴더: 파비콘(`favicon.svg`와 16·32·180px PNG)과 소셜 공유용 `og-image.png`(2400×1260, 브랜드 톤으로 직접 제작). 검색엔진·SNS가 파일로 직접 접근해야 해서 둔 예외이고, 그 외에는 계속 `index.html` 하나로 유지합니다.
- `sitemap.xml`: 검색엔진에 페이지 주소를 알리는 파일. 현재는 한 페이지짜리 사이트라 주소 하나만 들어 있습니다.

### index.html 안의 주요 부분
- `CONFIG` (스크립트 맨 위): 이름, Preply 주소, 블로그·유튜브 주소, 문의 이메일. 주소가 비어 있으면 해당 링크는 자동으로 숨겨지거나 비활성화됩니다.
- `T` 객체: 영어(`en`)와 한국어(`ko`) 문구. HTML의 `data-t="키"`가 이 문구로 채워집니다. **새 문구를 추가할 때는 두 언어를 항상 같이 추가하세요.**
- `VOWELS`, `WORDS`: 1단원 모음 설명과 첫 단어 데이터.
- `CONSONANTS`, `CONSONANT_WORDS`: 2단원 자음 설명과 단어 데이터. 자음은 가나다 순서(가나다라마바사아자차카타파하)로 정렬하고, 앞 9개(ㄱ–ㅈ)와 뒤 5개(ㅊ–ㅎ, 숨을 세게 내쉬는 소리)로 그룹을 나눕니다. ㅇ은 소리가 없어 퀴즈 출제 대상에서는 제외했습니다.
- 히어로의 음절 만들기(`buildChips`, `syllable`): 유니코드 한글 음절 공식 `0xAC00 + (초성*21 + 중성)*28 + 종성`을 사용합니다.
- 1단원 모음 탐색기(`renderVowel`), 단어 카드, 퀴즈(`startQuiz`, `renderQuiz`).
- 2단원 자음 탐색기(`renderConsonant`), 단어 카드, 퀴즈(`startQuiz2`, `renderQuiz2`). 1단원과 같은 구조를 그대로 따릅니다.
- `BATCHIM`, `BATCHIM_WORDS`: 3단원 받침 설명과 읽기 연습 단어 데이터. 받침 글자는 여러 개지만 실제로는 7개 대표음(ㄱㄴㄷㄹㅁㅂㅇ)으로만 발음된다는 점을 중심으로, 길게 끄는 소리(ㄴㄹㅁㅇ)와 짧게 끊기는 소리(ㄱㄷㅂ) 두 그룹으로 나눕니다. 다른 받침 글자(ㅅㅆㅈㅊㅌㅎ→ㄷ, ㅍ→ㅂ)가 같은 소리로 합쳐지는 규칙은 팁으로만 짧게 언급하고, 겹받침(자음군)은 다루지 않았습니다.
- 3단원 받침 탐색기(`renderBatchim`), 읽기 연습 단어, 퀴즈(`startQuiz3`, `renderQuiz3`). 1·2단원과 같은 구조를 그대로 따릅니다.
- 발음 듣기: 브라우저에 한국어 음성이 있을 때만 버튼이 나타납니다(`speechSynthesis`).

## 디자인 규칙
- 팔레트: 남색 잉크(`--ink`), 청자색(`--celadon`), 차가운 종이색(`--paper`), 주홍색 강조(`--cta`). 다크 모드 변수가 함께 정의되어 있습니다.
- 서체: 제목은 Gowun Batang(명조 계열), 본문은 Noto Sans KR.
- 기억에 남는 요소는 **한글 연습장 칸(십자 점선 칸)** 하나입니다. 나머지는 절제하고, 장식을 늘리지 마세요.
- 모바일 대응, 키보드 포커스 표시, `prefers-reduced-motion` 지원을 유지하세요.
- 모든 문구는 영어와 한국어가 모두 자연스러워야 합니다(번역기 투 금지).

## 현재 상태
완료:
- 히어로(음절 만들기), 무료 강의 목차, 1단원 모음(탐색기, 첫 단어, 퀴즈)
- 2단원 자음(탐색기, 단어, 퀴즈) — 1단원과 같은 틀로 추가
- 3단원 받침과 읽기(탐색기, 읽기 연습 단어 7개, 퀴즈) — 같은 틀로 추가. 이제 무료 강의 목차 3개 단원이 모두 열려 있습니다.
- 1·2·3단원 발음 설명 1차 검수(국립국어원 표준발음법 제8·9·12항 기준). 받침 ㄱ 설명에 "ㄲ, ㅋ도 같은 소리" 안내를 추가했고, 받침 ㄷ 설명에서 잘못 포함돼 있던 ㅎ을 뺐습니다(ㅎ 받침은 표준발음법 제9항의 단순 합류 대상이 아니라 제12항에 따라 뒤 자음과 만나 거센소리가 되거나 사라지는 등 다르게 움직입니다 — 아직 사이트에서 다루지 않음). 그 외 모음·자음 영어 비교 설명은 검토 결과 규칙상 문제는 없었지만, "비슷하게 들리는 느낌"에 대한 최종 확인은 여전히 한국어 발음 전문가 몫입니다.
- 더 배우기(전자책 3종, 영상 시리즈 1종, 모두 "준비 중"), 튜터링 신청, 소개, 자주 묻는 질문
- 영어/한국어 전환, 다크 모드
- **실제 공개 완료: https://danielzip.com/hangul/** — Hostinger hPanel로 운영자가 직접 업로드했고, 화면 확인 결과 히어로·음절 만들기·언어 전환(한국어)까지 정상 동작합니다. 워드프레스(같은 도메인의 `public_html`)와는 완전히 분리된 `/hangul` 폴더의 정적 페이지입니다. Claude 세션은 네트워크 정책상 `danielzip.com`에 직접 접속할 수 없어서, 이후 배포 확인은 운영자가 화면을 보고 알려주는 방식으로 진행합니다.

**브랜드 통일: "글월"** — 도메인 루트(워드프레스)를 한글·한국어·고대문화 콘텐츠 블로그 "글월"로 운영하기로 확정했고, `/hangul`도 같은 브랜드로 통일했습니다. `Hangul with Seongmin`/`{name}의 한글`이던 브랜드 표기(`<title>`, OG/Twitter 메타, `T.brand`)를 전부 "글월"로 바꿨고, `CONFIG.blogUrl`을 `https://danielzip.com/`로 채워 소개 섹션의 블로그 링크가 뜨게 했습니다. 워드프레스와 `/hangul`은 기술적으로는 계속 분리된 채로 두고(재구축 비용·코딩 부담 때문에 기술 통합은 하지 않음), 브랜드와 상호 링크로만 하나처럼 보이게 하는 방향입니다. 인물 이름(`CONFIG.name`: Seongmin/성민)은 소개 섹션 저자 표기이므로 그대로 두었습니다.

임시값(운영자가 나중에 채울 것):
- Preply 프로필 주소, 유튜브 주소 (아직 없음) — 블로그 주소는 위에서 채움
- 문의 이메일은 임시로 `small779961@gmail.com`이며, 사이트 전용 이메일을 만들면 교체
- 소개 문구와 사진은 임시(사진은 글자 칸으로 대체)
- 전자책·영상 제목과 설명은 샘플이며 가격은 없음
- 워드프레스(글월) 쪽 메뉴에 `/hangul/`로 가는 링크는 아직 없음 — 운영자가 워드프레스 관리자에서 직접 추가해야 함 (Claude는 워드프레스에 직접 접근 불가)

## 다음 작업 후보 (우선순위 순)
1. **(운영자 직접 진행, 선택)** 워드프레스 SEO 플러그인(Yoast/RankMath 등)이나 사이트 루트 `robots.txt`에 `Sitemap: https://danielzip.com/hangul/sitemap.xml` 한 줄을 추가하면 검색엔진이 더 빨리 찾습니다(루트 `robots.txt`는 워드프레스가 관리하므로 Claude가 직접 건드리지 않았습니다). Google Search Console에 `https://danielzip.com/hangul/`을 등록하는 것도 도움이 됩니다.
2. 겹받침(자음군, 예: ㄳㄵㄺ 등), 받침 ㅎ의 축약·거센소리되기(놓다→노타, 많고→만코 등), 연음(받침이 다음 음절 모음으로 이어지는 발음, 예: 국어→구거) — 3단원을 더 다루고 싶다면 다음 확장 후보.
3. 소개 문구와 사진 교체, 블로그·유튜브·Preply 주소 연결.
4. 튜터링 예약 자동화(Cal.com 등 예약 도구 연동)와 결제 수단 확인. 결제 서비스는 지원 국가 조건이 있어 운영자 상황에서 가능한지 먼저 확인해야 합니다.
5. 전자책이 완성되면 외부 판매처 링크와 가격 연결.
6. 사이트를 고칠 때마다: 로컬에서 확인한 새 `index.html`(또는 `assets/`)을 Hostinger hPanel 파일 관리자에서 같은 이름으로 다시 업로드(덮어쓰기)하면 반영됩니다. Claude는 `danielzip.com`에 직접 접속할 수 없으니, 배포 후 확인은 화면 캡처나 설명으로 부탁드립니다.

## 작업 방식 요청
- 한 번에 큰 변경을 하기보다, 작은 단위로 바꾸고 결과를 확인시켜 주세요.
- 변경 전에 무엇을 바꿀지 짧게 알려 주고, 되돌릴 수 있도록 git 커밋을 자주 남겨 주세요.
- 운영자가 정하지 않은 사업 결정(가격, 결제 방식 등)을 임의로 정해서 사이트에 넣지 마세요.
